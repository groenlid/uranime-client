{{#with view.content}}
<div class="row episodelist">
    <div class="pull-left span2">
        <div class="gallery-image">
            <a class="clickable" {{action showModal 'showepisode' this}}>
            <img {{bindAttr src="this.imageURL"}} /></a>
        </div>
    </div>
    <div class="span8 episode-info">
        <div class="row">
            <h4 class="span10 normal">
                <a class="clickable" {{action showEpisode this target="view"}}>
                    {{#if this.special}}
                        <span class="label label-info">Special</span>
                    {{/if}}
                    #{{this.number}} - {{this.name}}
                </a>
            </h4>
            <p class="subtle span2 pull-right">{{this.parseAired}}</p>
        </div>
        <p>{{this.shortDescription}}</p>
    </div>
</div>
<hr>
{{/with}}    
